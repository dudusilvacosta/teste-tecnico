backend:
  build:
    context: ./backend
  container_name: adonis-api
  ports:
    - "3333:3333"
  volumes:
    - ./backend:/app
    - /app/node_modules
  env_file:
    - ./backend/.env
  depends_on:
    - mysql
